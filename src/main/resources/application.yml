spring:
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true
  h2:
    console:
      enabled: true
      path: '/h2'
  sql:
    init:
      mode: always


logging:
  level:
    root: INFO